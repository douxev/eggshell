---
category: Actions
---

# IconButton

44px circular icon-only button — the trailing action on every app bar. `selected` fills both the glyph and its container.

```tsx
<IconButton name="settings" title="Réglages" />
```
