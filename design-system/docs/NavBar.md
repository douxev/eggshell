---
category: Navigation
---

# NavBar

The six-destination bottom bar. A pill and a filled glyph mark the active tab. The shipped app runs icon-only; `showLabels` adds captions.

```tsx
<NavBar items={items} active={tab} onNavigate={setTab} />
```
