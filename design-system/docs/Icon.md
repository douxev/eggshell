---
category: Foundations
---

# Icon

A single Material Symbols Rounded glyph, named by its ligature. The shipped font is subset to the icons the app uses. Inherits `color` from its parent, so tint it by setting colour on the wrapper.

```tsx
<Icon name="medication" size={26} fill />
```
