---
category: Inputs
---

# Segmented

Switches between mutually exclusive views. `joined` is the M3 segmented button; `pills` is the looser pair the photo screen uses.

```tsx
<Segmented value={v} onChange={setV} options={[{value:"g",label:"Galerie"},{value:"c",label:"Comparer"}]} />
```
