---
category: Inputs
---

# Switch

Material 3 switch. The thumb grows and gains a checkmark when on. Always give it a `label` when no visible text sits beside it.

```tsx
<Switch checked={on} onChange={setOn} label="Rappels" />
```
