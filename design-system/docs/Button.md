---
category: Actions
---

# Button

Pill-shaped Material 3 button in six variants and three sizes. `filled` is the primary action, `tonal`/`tonalpri` are secondary, `error` is destructive.

```tsx
<Button variant="filled" icon="check">Marquer comme prise</Button>
```
