---
category: Navigation
---

# ScreenHeader

The title block at the top of every screen. `large` is a top-level destination; `small` pairs with a back arrow on a pushed screen.

```tsx
<ScreenHeader title="Médications" action="settings" actionTitle="Réglages" />
```
