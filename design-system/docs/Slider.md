---
category: Inputs
---

# Slider

Chunky slider with a pill thumb — the control behind the app's four-axis mood entry. Tint each axis with `accent`.

```tsx
<Slider value={v} onChange={setV} accent="var(--tertiary)" label="Dysphorie" />
```
