---
category: Theming
---

# ThemeSwatchCard

One cell of the theme picker: a miniature of the palette with its name underneath, marked by a small egg that fills in when active. The preview paints itself from the real palette.

```tsx
<ThemeSwatchCard themeId="nord" label="Nord" selected onSelect={setTheme} />
```
